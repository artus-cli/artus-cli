import { ArtusCliConfig } from '../types';

export default {
  // binName: 'xxx',
  strict: true,
  inheritMetadata: true,
  // strictOptions: true,
  // strictCommands: true,
} satisfies ArtusCliConfig;
